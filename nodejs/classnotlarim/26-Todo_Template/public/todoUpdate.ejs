<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <title>Todos</title>
</head>

<body>
    <div class="container">
        <h1 class="mt-3"> FullStack Todo Template Project</h1>
        <hr>
        <div class="rows">
            <h2>TODO UPDATE</h2>
            <form method="POST" >
                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <td>Titile:</td>
                            <td> <input type="text" name="title" placeholder="Enter todo title"  class="form-control" value="<%= todo.title %>"/> </td>
                        </tr>
                        <tr>
                            <td>Description:</td>
                            <td> <textarea name="description"  placeholder="Enter todo title" class="form-control"><%= todo.description %></textarea></td>
                        </tr>
                        <tr>
                            <td>Priority:</td>
                            <td>
                                <select name="priority" class="form-select" >
                                    <% for( let key in priorities) { %>
                                        <option value="<%= key %>" <%= (todo.priority == key ? 'selected' : '') %> > <%= priorities[key] %> </option>
                                    <% } %>
                                </select>
                            </td>
                        </tr>
                        <tr>
                            <td>is Done:</td>
                            <td>
                                <select name="isDone" class="form-select" >
                                        <option value="1" <%= (todo.isDone ? 'selected' : '') %> > Yes </option>
                                        <option value="0" <%= (todo.isDone ? '' : 'selected') %> > No </option>
                                </select>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button class="btn btn-primary" type="submit">Update Todo</button>
            </form>
        </div>
    </div>
</body>

</html>